import { View } from "react-native";
import EclaPin from "./ECLA-pin";

<View
  style={{
    width: size,
    height: size,
    justifyContent: "center",
    alignItems: "center",
  }}
>
  <EclaPin width={size * 0.8} height={size * 0.8} fill={color} />
</View>;
